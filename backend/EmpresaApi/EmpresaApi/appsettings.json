// 🔹 Seção onde ficam as strings de conexão com o banco de dados.
// Cada chave aqui representa um nome de conexão que pode ser usado no projeto."ConnectionStrings": {
// 👉 'DefaultConnection' é o nome da conexão usada no seu AppDbContext.
// O valor "Data Source=empresa.db" indica que o banco de dados é um arquivo SQLite chamado empresa.db.
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=empresa.db"
  },
// Define o nível padrão de log para toda a aplicação.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
// Define quais hosts (domínios) podem acessar sua API.
  "AllowedHosts": "*"
}